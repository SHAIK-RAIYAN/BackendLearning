<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chat Details</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="chat-detail">
    <h1>Chat Details</h1>
    <p><strong>From:</strong> <%= chat.from %></p>
    <p><strong>To:</strong> <%= chat.to %></p>
    <p><strong>Message:</strong> <%= chat.msg %></p>
    <p><strong>Sent At:</strong> <%= chat.created_on.toLocaleString() %></p>

    <form action="/chats/<%= chat._id %>/edit" method="GET" style="display: inline;">
      <button class="edit-btn">✏️ Edit</button>
    </form>
    <form action="/chats/<%= chat._id %>?_method=DELETE" method="POST" style="display: inline;">
      <button class="delete-btn">🗑️ Delete</button>
    </form>

    <div style="margin-top: 20px;">
      <a href="/chats" class="cta-btn">🔙 Back to Chats</a>
    </div>
  </div>
</body>
</html>
